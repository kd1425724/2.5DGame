#include "EffectManager.h"
#include"../../Scene/SceneManager.h"

void EffectManager::AddEffect(EffectType type, Math::Vector3 pos, Math::Vector3 scale, int time)
{
	switch (type)
	{
	case EffectType::Explosion:
		break;
	default:
		break;
	}
}
